# Backend

Python backend 預計負責 API、workflow state、Agent tools、官方文件 retrieval、
deterministic eligibility rules、隱私界線與 AWS adapters。

FastAPI、Lambda handler、Strands Agents 與實際部署方式尚未定案，因此目前只建立
framework-neutral 的模組邊界與待實作檔案。
